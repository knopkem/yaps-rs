//! yaps-gui — GUI interface for the YAPS-RS photo sorter (iced).
//!
//! This is a stub that will be implemented in a later phase.

fn main() {
    eprintln!("yaps-gui: GUI not yet implemented. Use the CLI: `yaps --help`");
    std::process::exit(1);
}
